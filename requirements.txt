# Web framework
Flask==3.1.1

# Security: CSRF protection, rate limiting, HTML sanitisation
Flask-WTF==1.2.1
Flask-Limiter==3.5.0
bleach==6.1.0

# Markdown -> HTML rendering
Markdown==3.8.2

# Environment variables
python-dotenv==1.1.1

# Google Gemini SDK (pulls google-auth, pydantic, httpx, ... transitively)
google-genai==2.23.0

# WSGI server (used by Render/gunicorn; Vercel's Python runtime ignores it)
gunicorn==23.0.0
